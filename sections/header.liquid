<header
  id="site-header"
  class="sticky top-0 z-50 w-full font-serif bg-white transition-shadow duration-300"
>
  {% comment %} --- PC用レイアウト (lg以上) --- {% endcomment %}
  <div class="hidden lg:block">
    
    {% comment %} 
      ① 通常時の3段構成：
      header要素に .is-sticky クラスがつくと CSSで非表示になるよ
    {% endcomment %}
    <div class="header-normal-wrapper transition-all duration-300">
      {% comment %} 1段目：ユーティリティバー (背景：#C2A992) {% endcomment %}
      <div class="bg-[#C2A992] text-sm py-2">
        <div class="max-w-6xl mx-auto px-4 md:px-8 flex justify-between items-center">
          <div class="flex items-center space-x-4">
            <div class="relative" x-data="{ open: false }">
              <button @click="open = !open" @click.away="open = false" class="flex items-center bg-white px-3 py-1 border border-gray-300 rounded text-gray-700">
                <span>マイメニュー</span>
                <span class="ml-1 w-4 h-4 text-gray-400">{% render 'icon-chevron-down' %}</span>
              </button>
              <div x-show="open" x-cloak class="absolute left-0 mt-2 w-40 bg-white border shadow-lg py-2 z-50">
                <a href="/account" class="block px-4 py-2 hover:bg-gray-100">ログイン</a>
                <a href="/account" class="block px-4 py-2 hover:bg-gray-100">注文履歴</a>
              </div>
            </div>
            <a href="/account/register" class="bg-white px-3 py-1 border border-gray-300 rounded text-gray-700">新規会員登録</a>
          </div>
          <div class="flex items-center">
            <a href="{{ routes.cart_url }}" class="relative text-white">
              <div class="w-8 h-8">{% render 'icon-cart' %}</div>
              {% if cart.item_count > 0 %}
                <span class="absolute -top-1 -right-1 bg-red-500 text-white text-[10px] font-bold rounded-full w-4 h-4 flex items-center justify-center">{{ cart.item_count }}</span>
              {% endif %}
            </a>
          </div>
        </div>
      </div>

      {% comment %} 2段目：ロゴ & 検索バー (背景：ホワイト) {% endcomment %}
      <div class="bg-white py-4">
        <div class="max-w-6xl mx-auto px-4 md:px-8 relative flex items-center h-16">
          <div class="flex-shrink-0 bg-[#FDFBF7] px-4 py-2 z-10">
            <a href="/" class="flex items-center">
              {% if section.settings.logo != blank %}
                <img src="{{ section.settings.logo | image_url: width: 500 }}" alt="{{ section.settings.logo.alt }}" style="width: {{ section.settings.logo_width }}px;" class="h-auto">
              {% else %}
                <span class="text-2xl font-serif font-bold text-[#C2A992] tracking-wider ml-1">Your day</span>
              {% endif %}
            </a>
          </div>
          <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
            <form action="{{ routes.search_url }}" method="get" class="relative flex items-center border border-gray-400 rounded overflow-hidden w-full max-w-md pointer-events-auto">
              <input type="text" name="q" placeholder="{{ section.settings.search_placeholder }}" class="w-full px-4 py-2 outline-none text-sm">
              <button type="submit" class="bg-gray-400 p-2 text-white">
                <div class="w-5 h-5">{% render 'icon-search' %}</div>
              </button>
            </form>
          </div>
        </div>
      </div>

      {% comment %} 3段目：ナビゲーション (背景：#EAE1D8) {% endcomment %}
      <nav class="bg-[#EAE1D8] border-t border-b border-black/5">
        <div class="max-w-6xl mx-auto px-4 md:px-8 relative flex items-center h-12">
          <div class="flex-grow flex justify-center space-x-12">
            <span class="text-sm font-medium text-gray-800">text</span>
            <span class="text-sm font-medium text-gray-800">text</span>
            <span class="text-sm font-medium text-gray-800">text</span>
            <span class="text-sm font-medium text-gray-800">text</span>
            <span class="text-sm font-medium text-gray-800">text</span>
          </div>
          <div class="absolute right-4 md:right-8">
            <a href="#" class="text-sm font-medium text-gray-800 hover:text-[#C2A992]">お買い物ガイド</a>
          </div>
        </div>
      </nav>
    </div>

    {% comment %} 
      ② スクロール固定時の1段レイアウト：
      header要素に .is-sticky クラスがついた時だけ CSSで表示されるよ
    {% endcomment %}
    <div class="header-sticky-wrapper hidden bg-white/95 backdrop-blur-md border-b border-gray-200 py-2 shadow-sm">
      <div class="max-w-6xl mx-auto px-4 md:px-8 relative flex items-center justify-between">
        <div class="flex items-center space-x-6 z-10 flex-shrink-0">
          <a href="/" class="flex-shrink-0">
            {% if section.settings.logo != blank %}
              <img src="{{ section.settings.logo | image_url: width: 200 }}" alt="{{ section.settings.logo.alt }}" style="width: 70px;" class="h-auto">
            {% else %}
              <span class="text-xl font-serif font-bold text-[#C2A992]">Your day</span>
            {% endif %}
          </a>
          <form action="{{ routes.search_url }}" method="get" class="relative flex items-center border border-gray-300 rounded overflow-hidden w-48">
            <input type="text" name="q" placeholder="検索" class="w-full px-3 py-1 outline-none text-xs">
            <button type="submit" class="bg-gray-400 px-2 py-1 text-white">
              <div class="w-4 h-4">{% render 'icon-search' %}</div>
            </button>
          </form>
        </div>

        <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
          <div class="flex space-x-10 pointer-events-auto">
            <span class="text-sm font-medium text-gray-700">text</span>
            <span class="text-sm font-medium text-gray-700">text</span>
            <span class="text-sm font-medium text-gray-700">text</span>
            <span class="text-sm font-medium text-gray-700">text</span>
            <span class="text-sm font-medium text-gray-700">text</span>
          </div>
        </div>

        <div class="flex items-center z-10">
          <a href="{{ routes.cart_url }}" class="relative text-gray-700">
            <div class="w-7 h-7">{% render 'icon-cart' %}</div>
            {% if cart.item_count > 0 %}
              <span class="absolute -top-1 -right-1 bg-red-500 text-white text-[9px] font-bold rounded-full w-4 h-4 flex items-center justify-center">{{ cart.item_count }}</span>
            {% endif %}
          </a>
        </div>
      </div>
    </div>
  </div>

  {% comment %} --- スマホ用レイアウト (変更なし) --- {% endcomment %}
  <div class="lg:hidden bg-[#FDFBF7]/90 backdrop-blur-md border-b border-[#C2A992]/20 h-20 flex items-center justify-between px-6" x-data="{ mobileMenuOpen: false }">
    <a href="/" class="flex items-center">
      {% if section.settings.logo != blank %}
        <img src="{{ section.settings.logo | image_url: width: 500 }}" alt="{{ section.settings.logo.alt }}" style="width: {{ section.settings.logo_width }}px;" class="h-auto">
      {% else %}
        <span class="text-2xl font-serif font-bold text-[#C2A992] tracking-wider ml-1">Your day</span>
      {% endif %}
    </a>
    <button @click="mobileMenuOpen = !mobileMenuOpen" type="button" class="text-gray-600 p-2">
      {% render 'icon-hambarger' %}
    </button>
    <div x-show="mobileMenuOpen" x-cloak class="absolute top-20 left-0 w-full bg-[#FDFBF7] border-t border-[#C2A992]/10 min-h-screen">
      <nav class="px-4 py-6 space-y-4">
        <span class="block py-2 text-lg text-gray-700 border-b border-[#C2A992]/5">text</span>
        <span class="block py-2 text-lg text-gray-700 border-b border-[#C2A992]/5">text</span>
        <span class="block py-2 text-lg text-gray-700 border-b border-[#C2A992]/5">text</span>
        <span class="block py-2 text-lg text-gray-700 border-b border-[#C2A992]/5">text</span>
        <span class="block py-2 text-lg text-gray-700 border-b border-[#C2A992]/5">text</span>
      </nav>
    </div>
  </div>
</header>

<style>
  [x-cloak] { display: none !important; }
  /* 固定時の出し分け用CSS */
  #site-header.is-sticky .header-normal-wrapper { display: none; }
  #site-header.is-sticky .header-sticky-wrapper { display: block; }
  #site-header.is-sticky { border-bottom:

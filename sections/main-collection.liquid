<nav class="mb-8 text-center mt-8">
  <a
    href="{{ routes.collections_url }}"
    class="text-base text-gray-600 hover:text-[#C2A992] transition-colors inline-flex items-center gap-2"
  >
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
    </svg>
    コレクション一覧に戻る</a
  >
</nav>

<!-- コレクションタイトル -->
<h1 class="text-2xl md:text-3xl font-bold text-center mb-8 text-gray-800">
  {% if section.settings.custom_title != blank %}
    {{ section.settings.custom_title }}
  {% else %}
    {{ collection.title }}
  {% endif %}
</h1>

<section class="max-w-6xl mx-auto px-4 py-12 md:px-8">
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-8">
    <div class="flex flex-col items-center">
      <a href="/collections/relaxation" class="w-full group">
        <div class="aspect-[4/5] bg-gray-100 rounded-2xl overflow-hidden relative">
          {% comment %}
            <img
              src="//yourday-dev-p.myshopify.com/cdn/shop/collections/relax.png?v=1767608180&amp;width=600"
              alt="リラクゼーション"
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
              loading="lazy"
            >
          {% endcomment %}
          {{ 'collection-1' | placeholder_svg_tag: 'w-full h-full bg-gray-200' }}

          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/20 transition-colors duration-300"></div>

          <div class="absolute inset-0 flex items-center justify-center p-4">
            <h3 class="text-white text-base md:text-lg font-serif tracking-widest text-center drop-shadow-md">
              リラクゼーション
            </h3>
          </div>
        </div>
      </a>
    </div>

    <div class="flex flex-col items-center">
      <a href="/collections/bath" class="w-full group">
        <div class="aspect-[4/5] bg-gray-100 rounded-2xl overflow-hidden relative">
          <img
            src="//yourday-dev-p.myshopify.com/cdn/shop/collections/bath.png?v=1767608082&amp;width=600"
            alt="お風呂でケア"
            class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
            loading="lazy"
          >

          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/20 transition-colors duration-300"></div>

          <div class="absolute inset-0 flex items-center justify-center p-4">
            <h3 class="text-white text-base md:text-lg font-serif tracking-widest text-center drop-shadow-md">
              お風呂でケア
            </h3>
          </div>
        </div>
      </a>
    </div>

    <div class="flex flex-col items-center">
      <a href="/collections/room" class="w-full group">
        <div class="aspect-[4/5] bg-gray-100 rounded-2xl overflow-hidden relative">
          <img
            src="//yourday-dev-p.myshopify.com/cdn/shop/collections/room.png?v=1767608142&amp;width=600"
            alt="お部屋のケア"
            class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
            loading="lazy"
          >

          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/20 transition-colors duration-300"></div>

          <div class="absolute inset-0 flex items-center justify-center p-4">
            <h3 class="text-white text-base md:text-lg font-serif tracking-widest text-center drop-shadow-md">
              お部屋のケア
            </h3>
          </div>
        </div>
      </a>
    </div>

    <div class="flex flex-col items-center">
      <a href="/collections/hands" class="w-full group">
        <div class="aspect-[4/5] bg-gray-100 rounded-2xl overflow-hidden relative">
          <img
            src="//yourday-dev-p.myshopify.com/cdn/shop/collections/15.png?v=1767608263&amp;width=600"
            alt="ハンドケア"
            class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
            loading="lazy"
          >

          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/20 transition-colors duration-300"></div>

          <div class="absolute inset-0 flex items-center justify-center p-4">
            <h3 class="text-white text-base md:text-lg font-serif tracking-widest text-center drop-shadow-md">
              ハンドケア
            </h3>
          </div>
        </div>
      </a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main Collection",
  "settings": [
    {
      "type": "text",
      "id": "custom_title",
      "label": "タイトル",
      "info": "例: 商品、おすすめ商品など"
    },
    {
      "type": "range",
      "id": "number_of_products_per_page",
      "min": 8,
      "max": 48,
      "step": 4,
      "label": "1ページあたりの商品数",
      "default": 12
    }
  ]
}
{% endschema %}

{% comment %}
  <div class="py-8">
    <div class="max-w-6xl mx-auto px-4 md:px-8">
      <!-- パンくずナビ -->
      <nav class="mb-8 text-center">
        <a
          href="{{ routes.collections_url }}"
          class="text-sm text-gray-600 hover:text-[#C2A992] transition-colors inline-flex items-center gap-2"
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
          </svg>
          コレクション一覧に戻る
        </a>
      </nav>

      {% paginate collection.products by section.settings.number_of_products_per_page %}
        <!-- コレクションタイトル -->
        <h1 class="text-3xl md:text-4xl font-bold text-center mb-8 text-gray-800">
          {% if section.settings.custom_title != blank %}
            {{ section.settings.custom_title }}
          {% else %}
            {{ collection.title }}
          {% endif %}
        </h1>

        <!-- コレクション説明 -->
        {% if collection.description != blank %}
          <div class="max-w-3xl mx-auto mb-12 text-center text-gray-600">
            {{ collection.description }}
          </div>
        {% endif %}

        <!-- 商品グリッド -->
        {% if collection.products.size > 0 %}
          <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6">
            {% for product in collection.products %}
              {% render 'product-card', product: product, collection: collection %}
            {% endfor %}
          </div>

          <!-- ページネーション -->
          {% if paginate.pages > 1 %}
            <div class="mt-8">
              {% render 'pagination', paginate: paginate %}
            </div>
          {% endif %}
        {% else %}
          <div class="text-center py-16">
            <p class="text-xl text-gray-500">このコレクションには商品がまだ登録されていません</p>
            <a
              href="{{ routes.collections_url }}"
              class="mt-6 inline-block px-6 py-3 bg-gray-800 text-white rounded hover:bg-gray-700 transition-colors"
            >
              他のコレクションを見る
            </a>
          </div>
        {% endif %}
      {% endpaginate %}
    </div>
  {% comment %} </div> {% endcomment %}

  {% schema %}
  {
    "name": "Main Collection",
    "settings": [
      {
        "type": "text",
        "id": "custom_title",
        "label": "タイトル",
        "info": "例: 商品、おすすめ商品など"
      },
      {
        "type": "range",
        "id": "number_of_products_per_page",
        "min": 8,
        "max": 48,
        "step": 4,
        "label": "1ページあたりの商品数",
        "default": 12
      }
    ]
  }
  {% endschema %}
{% endcomment %}
